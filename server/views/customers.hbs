<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
    <head>
        {{> header}}
        <title>Customers</title>
    </head>
    <body>
        {{> menu}}
        <h1 class="texto_centrado">Customers</h1>
        <div class="contenido">
            <ul>
                {{#each customer}}
                    <li>{{first_name}} {{last_name}}</li>
                {{/each}}
            </ul>
        </div>
        {{> footer}}
    </body>
</html>